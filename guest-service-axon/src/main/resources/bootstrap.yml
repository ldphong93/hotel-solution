spring:
  cloud:
    config:
      uri: http://localhost:8888
      username: admin-phong
      password: pass-phong
  config:
    activate:
      on-profile: security
  application:
    name: guest-service-axon